# HXZ News Reader — X 抓取任务交接（2026-06-10 → 2026-06-11）

> 这份文档是给**明天新 session 的 Claude**看的。读完即可接手，不需要回看历史。

---

## 用户身份与协作规则

- 用户：黄晓泽（HXZ），北京商科 IP 运营，**产品经理身份，不懂技术**
- 沟通：讲用户任务，**禁止堆 RSS/Docker/API/SDK/Webhook 这类术语**，必须用括号翻译
- 风格：贝叶斯 + 找杠杆点 + 不迎合 + 循序渐进一点点答
- 能自己决定的就直接做，不要反复追问；但**风险操作必须先确认**

---

## 项目背景

### 真相源（单一权威）

**飞书内容工程文档**：https://tcnih12gszer.feishu.cn/wiki/W5HTw1hAOiTc57kLCYEcQ5H0nLf

skill 与代码的信源必须以这份文档为准，不要自己加减。

### 用户任务（用户的话）

> 每天打开一个网页，能看到我订阅的 X / 公众号 / 播客 / 商科信源的最近 24-48 小时内容（原文 + 缩略图），按账号或时间分组。

**当前阶段：只做 X 渠道**。其他渠道（公众号 / 播客 / 商科）暂时不动。

### 4 条硬标准（不满足直接砍）

1. **稳定** — 不要三天两头坏
2. **免费** — 只消耗 Claude Code 订阅 token，零外部付费 API
3. **原文一致** — 推文文本 + 图必须是 X 原文，不能 LLM 改写
4. **速度** — 可接受范围（具体阈值用户未给，但 5 分钟内即可）

---

## 今天（2026-06-10）已经发生的事

### 全局决策：放弃 76 次循环抓取，改用 X List timeline 一次调用

- **旧方案**（已废弃）：用 twitter-cli 循环抓 76 个 handle，到第 50 个就触发 X 的 50 次/15 分钟限速墙，44 成功 / 10 失败
- **新方案**（已部署）：把 76 个 handle 加进一个 X Private List，每天用 twscrape 的 `list_timeline` 抓一次，**一次请求拿回全部成员的最新推文**

### List 信息

- **List ID**：`2064609881357975740`
- **List URL**：https://x.com/i/lists/2064609881357975740
- **类型**：Private（私有，只有用户自己看得到）

### 今天的进度

| 项 | 状态 |
|---|---|
| 装 twscrape + 灌 cookie（账号名 `hxz_main`）+ 配代理 | ✅ |
| 写 `scripts/x_list/add_members.py`（绕过 twikit XCTI 失效，直接调 X GraphQL ListAddMember） | ✅ |
| 加成员：成功 **42/76 = 55%** | ⏸️ 被 X 限速触发"连续 3 失败即停"保护 |
| 重写 `scripts/update_news.py` 走 `fetch_x_list()` 一次调用 | ✅ |
| 端到端测试（list 里 13 人时）：70.5 秒抓回 190 条推文 | ✅ |
| 删 526 → 432 行（清掉 94 行死代码：cookie_probe / load_x_state / 老 fetch_x_handle） | ✅ |
| 42 人完整端到端打分 | ❌ 明天做（cookie 需冷却 24h） |
| 推 GitHub | ❌ 明天做 |

### 加成员失败清单（4 个）

| Handle | 原因 | 处理 |
|---|---|---|
| `elevenlabsio` (index 9) | X 上账号改名 / 不存在 | 明天 sources.yaml 里查替代名（如 `elevenlabsio` → `elevenlabs`）或删除 |
| `LinusEkenstam` (43) | twscrape user_by_login 超时 60s | 明天续跑重试 |
| `nickfloats` (44) | 同上 | 同上 |
| `heatherbcooper` (45) | 同上 | 同上 |

后 3 个是限速 timeout 而不是真不存在，**明天 cookie 冷却后大概率能成**。

---

## 明天（2026-06-11）你（新 session）要做的事

### Step 1：先读 hxz-news-reader skill

```
/hxz-news-reader
```

不需要重复探索文件结构，skill 文档已经定义了 sources.yaml / update_news.py / GitHub Pages 的工作流。

### Step 2：把剩下 34 个 handle 加进 List

**命令（直接粘贴跑）：**

```bash
cd /Users/admin/Downloads/hxz-news-reader
/Users/admin/.local/share/uv/tools/twscrape/bin/python3 \
  scripts/x_list/add_members.py --list-id 2064609881357975740 \
  --start 46 --end 76 --sleep 30 > /tmp/add_members_day2.log 2>&1 &
```

参数解释：
- `--start 46 --end 76` → 处理 index 46-75 共 30 个（44-45 是昨天 timeout 的，建议先单独补这俩，见 Step 2.5）
- `--sleep 30` → 比昨天 15 秒翻倍，更稳
- 预计耗时 ~21 分钟（30 × (12s user_id + 30s sleep)）
- 后台跑，可继续做其他事

### Step 2.5：补昨天 3 个 timeout 的（可选，先 Step 2 跑完再做）

```bash
# 单独补 43-45
/Users/admin/.local/share/uv/tools/twscrape/bin/python3 \
  scripts/x_list/add_members.py --list-id 2064609881357975740 \
  --start 43 --end 46 --sleep 60
```

`--sleep 60` 因为这 3 个昨天炸过 timeout，给最大安全间隔。

### Step 2.6：处理 elevenlabsio

不要重新跑 add_members 试它，先去 X 上确认它真名：

```bash
# 不调 X API 也能查的方式（agent-reach 或 webfetch）
# 或者就在 sources.yaml 里把 elevenlabsio 改成正确名字
```

`/Users/admin/Downloads/hxz-news-reader/feeds/sources.yaml` 里搜 `elevenlabsio` 改名后再加 list。

### Step 3：端到端打分（**全天唯一一次 X 调用**）

```bash
cd /Users/admin/Downloads/hxz-news-reader
/Users/admin/.local/share/uv/tools/twscrape/bin/python3 \
  scripts/update_news.py --enable-x --window-hours 48 --x-per-handle 30
```

预期输出：
```
[hxz-news-reader] X list_timeline list_id=2064609881357975740 handles=76
[hxz-news-reader] X 完成: items=XXX 耗时 XXs
[hxz-news-reader] sources=110 ok=110 failed=0 items=XXX
```

**记录这次的：耗时秒数 / X items 数量 / 失败 handle 数。** 是最终打分的核心证据。

### Step 4：4 标准最终打分

按昨天的模板填实测数据，输出 markdown 表格给用户：

| 标准 | 结果 | 实测证据 |
|---|---|---|
| 稳定 | ? | 76 人耗时 + 失败率 |
| 免费 | ? | 全程 cookie，零付费 |
| 原文一致 | ? | 抽 3 条推文回 X 原推核对 |
| 速度 | ? | 实测秒数 |

### Step 5：更新 skill 文档 + 推 GitHub

**skill 文档** `/Users/admin/.claude/skills/hxz-news-reader/SKILL.md`（或同级文件）需要补：
- 新的 X List 工作流（list_id / twscrape / fetch_x_list）
- 替代掉旧的 twitter-cli 循环说明
- 加成员脚本的用法
- 限速教训：单天加成员上限 ~40，剩余分天

**推 GitHub**：
```bash
cd /Users/admin/Downloads/hxz-news-reader
git add -A
git status   # ← 先 review，确认没有 cookie / token 泄漏
git commit -m "X 渠道改用 List timeline 一次调用，废弃 76 handle 循环抓取"
git push
```

GitHub 仓库：https://github.com/blandybaran527-boop/Content-Engineering（如未确认，先 `git remote -v` 看）

---

## 关键约束（不要踩坑）

### 不要做的事

- ❌ **不要并发跑多个 twscrape 命令**（单 cookie 池，sqlite 并发会冲突）
- ❌ **不要短时间内重复跑 update_news.py 测试**（每次都消耗 X quota）
- ❌ **不要把 cookie 写进代码 / 仓库**（环境变量 `TWITTER_AUTH_TOKEN` / `TWITTER_CT0`）
- ❌ **不要凭印象改 sources.yaml**（飞书是真相源，每次改前先对齐）
- ❌ **不要走 twikit 加成员**（X 改前端后 twikit 的 client-transaction-id 算法已失效，已绕开）

### 要做的事

- ✅ **每次 X 调用都要有目的**（每一次都消耗 quota）
- ✅ **加成员失败 ≥3 个连续超时立刻停**（脚本已自带保护，不要绕过）
- ✅ **跑完一次端到端就 4 标准打分**（不要分多次跑测试）
- ✅ **改 skill 文档之前先看现有内容**，增量补充，不要全部重写

---

## 环境信息（不需要重新探索）

### 工具链

| 工具 | 路径 / 用途 |
|---|---|
| twscrape | `/Users/admin/.local/share/uv/tools/twscrape/bin/twscrape` (v0.18.1) |
| twscrape DB | `/Users/admin/accounts.db`（已配 cookie + 代理） |
| Python（含全部依赖） | `/Users/admin/.local/share/uv/tools/twscrape/bin/python3` (3.14) |
| 代理 | `http://127.0.0.1:7890`（已写进 twscrape DB 的 proxy 字段） |
| Cookie 环境变量 | `TWITTER_AUTH_TOKEN`、`TWITTER_CT0`（已设） |

### 关键文件

| 文件 | 用途 |
|---|---|
| `/Users/admin/Downloads/hxz-news-reader/feeds/sources.yaml` | 信源清单（76 个 X + 公众号 + 播客 + 商科） |
| `/Users/admin/Downloads/hxz-news-reader/scripts/update_news.py` | 主抓取脚本（已重写） |
| `/Users/admin/Downloads/hxz-news-reader/scripts/x_list/add_members.py` | 一次性加 list 成员脚本 |
| `/Users/admin/Downloads/hxz-news-reader/index.html` | 60 行展示页面（PM 已接受为 v0 基线，后续再升级） |
| `/Users/admin/Downloads/hxz-news-reader/data/items.json` | update_news.py 输出，前端读这个 |
| `/Users/admin/Downloads/hxz-news-reader/data/source-status.json` | 每个 source 的健康度 |
| `/tmp/add_members.log` | 昨天加成员日志（含 4 个失败明细） |

### 如何看当前 list 里有谁（**只在必要时**调，会消耗 X quota）

```bash
twscrape --db /Users/admin/accounts.db list_members 2064609881357975740 --limit 100 \
  | grep -o '"username": "[^"]*"' | sort -u
```

---

## 用户的 4 条铁律（贯穿协作）

1. **不要凭印象**：要 GitHub 真实数据 / 飞书原文 / 命令实际输出
2. **不要堆术语**：PM 听不懂的词都要翻译
3. **不要跳步**：一次一件，确认完再下一个
4. **不要烧 cookie**：每次 X 调用都要有目的，限速来了就停

---

## 当前 4 标准初步打分（昨天 13 人数据）

| 标准 | 分 | 证据 |
|---|---|---|
| 稳定 | ✅ | 1 次调用零 429 |
| 免费 | ✅ | 全程 cookie，零付费 |
| 原文一致 | ✅ | rawContent 直出，t.co 清理 |
| 速度 | ✅ | 70.5 秒 / 13 人 / 190 条 |

明天用 76 人覆盖实测后给最终分。

---

## 如果用户开口第一句是「继续」

按本文档 Step 1 → Step 5 顺序执行。Step 3 是关键节点（消耗 X quota），跑前要先把 Step 2 / Step 2.5 / Step 2.6 全部跑完确认。

如果用户问"昨天到哪了"，直接答：
> "List 里 42/76 = 55%，被昨天 cookie 限速卡停。Cookie 应该已冷却。计划：先续跑剩下 34 个加成员，再跑 1 次端到端做最终 4 标准打分。要开始吗？"
